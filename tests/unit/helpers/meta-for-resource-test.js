
import { metaForResource } from 'dummy/helpers/meta-for-resource';
import { module, test } from 'qunit';

module('Unit | Helper | meta for resource');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = metaForResource([42]);
  assert.ok(result);
});

